<h1>Editing <%= @story.title%></h1>

<%= render 'form' %>

<%= link_to 'Destroy', admin_story_path(@story), method: :delete, data: { confirm: 'Are you sure?' } %>
<%= link_to 'Back', admin_stories_path %>

