<%= form_for(@fr_tweet) do |f| %>
  <% if @fr_tweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fr_tweet.errors.count, "error") %> prohibited this fr_tweet from being saved:</h2>

      <ul>
      <% @fr_tweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
